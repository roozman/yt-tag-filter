{
  "name": "youtube-tag-filter",
  "version": "1.0.0",
  "private": true,
  "description": "A minimal Chrome and Firefox extension that only reveals YouTube videos matching user-defined tags.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check": "node --check src/matcher.js && node --check src/content.js && node --check src/options.js && npm test && npm run build"
  }
}

